import React from "react";
import { StyleSheet, Text, View } from "react-native";

interface Props {}

export default function CircularItems({}: Props) {
  return (
    <View>
      <Text>CircularItems</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
